name: fingerprint-yaml-Kangle虚拟主机
manual: false
detail:
    fingerprint:
        name: Kangle虚拟主机
    fofa: title="Kangle虚拟主机"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("Kangle虚拟主机")
expression: r0()
