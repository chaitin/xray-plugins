name: fingerprint-yaml-深优通
manual: false
detail:
    fingerprint:
        name: 深优通
    fofa: title="深优通"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("深优通")
expression: r0()
