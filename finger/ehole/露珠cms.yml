name: fingerprint-yaml-露珠cms
manual: false
detail:
    fingerprint:
        name: 露珠cms
    fofa: title="露珠cms"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("露珠cms")
expression: r0()
