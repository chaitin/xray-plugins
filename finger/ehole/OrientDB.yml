name: fingerprint-yaml-OrientDB
manual: false
detail:
    fingerprint:
        name: OrientDB
    fofa: title="redirecting to orientdb"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("redirecting to orientdb")
expression: r0()
