name: fingerprint-yaml-Maybecms
manual: false
detail:
    fingerprint:
        name: Maybecms
    fofa: title="Maybecms"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("Maybecms")
expression: r0()
