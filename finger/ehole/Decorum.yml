name: fingerprint-yaml-Decorum
manual: false
detail:
    fingerprint:
        name: Decorum
    fofa: title="Decorum"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("Decorum")
expression: r0()
