name: fingerprint-yaml-速致
manual: false
detail:
    fingerprint:
        name: 速致
    fofa: title="速致"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("速致")
expression: r0()
