name: fingerprint-yaml-ThinkJS
manual: false
detail:
    fingerprint:
        name: ThinkJS
    fofa: title="ThinkJS"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("ThinkJS")
expression: r0()
