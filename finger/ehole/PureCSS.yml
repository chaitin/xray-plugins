name: fingerprint-yaml-PureCSS
manual: false
detail:
    fingerprint:
        name: PureCSS
    fofa: title="Pure CSS"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("Pure CSS")
expression: r0()
