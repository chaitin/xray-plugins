name: fingerprint-yaml-RainMachine-Company's-product
manual: false
detail:
    fingerprint:
        name: RainMachine-Company's-product
    fofa: title="rainmachine"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("rainmachine")
expression: r0()
