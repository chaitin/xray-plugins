name: fingerprint-yaml-致远OA M3 Server V2_0
manual: false
detail:
    fingerprint:
        name: 致远OA M3 Server V2.0
    fofa: title="M3 Server V2.0"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("M3 Server V2.0")
expression: r0()
