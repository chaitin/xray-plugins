name: fingerprint-yaml-tapenjoy-Manager
manual: false
detail:
    fingerprint:
        name: tapenjoy-Manager
    fofa: title="余香经分管理系统"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("余香经分管理系统")
expression: r0()
