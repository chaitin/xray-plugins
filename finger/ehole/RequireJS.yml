name: fingerprint-yaml-RequireJS
manual: false
detail:
    fingerprint:
        name: RequireJS
    fofa: title="RequireJS"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("RequireJS")
expression: r0()
