name: fingerprint-yaml-WPS 部署可视化平台
manual: false
detail:
    fingerprint:
        name: WPS 部署可视化平台
    fofa: title="WPS 部署可视化平台"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("WPS 部署可视化平台")
expression: r0()
