name: fingerprint-yaml-WebOptimizer
manual: false
detail:
    fingerprint:
        name: WebOptimizer
    fofa: title="Web Optimizer"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("Web Optimizer")
expression: r0()
