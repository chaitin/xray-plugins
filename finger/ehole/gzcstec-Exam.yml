name: fingerprint-yaml-gzcstec-Exam
manual: false
detail:
    fingerprint:
        name: gzcstec-Exam
    fofa: body="placeholder=\"请输入凭据\""
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.body_string.contains('placeholder="请输入凭据"')
expression: r0()
