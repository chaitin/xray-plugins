name: fingerprint-yaml-Google-Hosted-Libraries
manual: false
detail:
    fingerprint:
        name: Google-Hosted-Libraries
    fofa: header="ajax.googleapis.com/ajax/libs" || body="ajax.googleapis.com/ajax/libs"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.raw_header.bcontains(bytes("ajax.googleapis.com/ajax/libs"))
    r1:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.body_string.contains("ajax.googleapis.com/ajax/libs")
expression: r0() || r1()
