name: fingerprint-yaml-Purelove
manual: false
detail:
    fingerprint:
        name: Purelove
    fofa: title="Purelove"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("Purelove")
expression: r0()
