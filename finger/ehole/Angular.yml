name: fingerprint-yaml-Angular
manual: false
detail:
    fingerprint:
        name: Angular
    fofa: title="be angular | bootstrap admin web app with angularjs"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("be angular | bootstrap admin web app with angularjs")
expression: r0()
