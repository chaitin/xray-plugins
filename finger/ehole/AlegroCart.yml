name: fingerprint-yaml-AlegroCart
manual: false
detail:
    fingerprint:
        name: AlegroCart
    fofa: title="AlegroCart"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("AlegroCart")
expression: r0()
