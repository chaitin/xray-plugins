name: fingerprint-yaml-众拓
manual: false
detail:
    fingerprint:
        name: 众拓
    fofa: title="众拓"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("众拓")
expression: r0()
