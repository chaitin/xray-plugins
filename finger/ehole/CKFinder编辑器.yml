name: fingerprint-yaml-CKFinder编辑器
manual: false
detail:
    fingerprint:
        name: CKFinder编辑器
    fofa: title="CKFinder编辑器"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("CKFinder编辑器")
expression: r0()
