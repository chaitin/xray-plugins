name: fingerprint-yaml-星域cdn
manual: false
detail:
    fingerprint:
        name: 星域cdn
    fofa: title="星域cdn"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("星域cdn")
expression: r0()
