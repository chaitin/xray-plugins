name: fingerprint-yaml-CP_PLUS-NVR
manual: false
detail:
    fingerprint:
        name: CP_PLUS-NVR
    fofa: title="cpplus nvr –web view"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("cpplus nvr –web view")
expression: r0()
