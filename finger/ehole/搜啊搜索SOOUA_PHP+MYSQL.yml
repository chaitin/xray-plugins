name: fingerprint-yaml-搜啊搜索SOOUA_PHP+MYSQL
manual: false
detail:
    fingerprint:
        name: 搜啊搜索SOOUA_PHP+MYSQL
    fofa: title="搜啊搜索SOOUA_PHP+MYSQL"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("搜啊搜索SOOUA_PHP+MYSQL")
expression: r0()
