name: fingerprint-yaml-UEditor
manual: false
detail:
    fingerprint:
        name: UEditor
    fofa: title="UEditor"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("UEditor")
expression: r0()
