name: fingerprint-yaml-Stamplay
manual: false
detail:
    fingerprint:
        name: Stamplay
    fofa: title="Stamplay"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("Stamplay")
expression: r0()
