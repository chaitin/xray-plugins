name: fingerprint-yaml-云因信息
manual: false
detail:
    fingerprint:
        name: 云因信息
    fofa: title="云因信息"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("云因信息")
expression: r0()
