name: fingerprint-yaml-QUANTIL
manual: false
detail:
    fingerprint:
        name: QUANTIL
    fofa: title="QUANTIL"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("QUANTIL")
expression: r0()
