name: fingerprint-yaml-同兴万点
manual: false
detail:
    fingerprint:
        name: 同兴万点
    fofa: title="同兴万点"
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /
            follow_redirects: true
        expression: response.title_string.contains("同兴万点")
expression: r0()
